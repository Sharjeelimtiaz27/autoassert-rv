# autoassert-rv
AutoAssert-RV is a research pipeline that leverages large language models (LLMs) to automatically generate formal assertions from RTL (Register-Transfer Level) hardware designs. The tool targets hardware security.
